meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 18

build:
  command: npm install
  additionalCommands: echo "Зависимости установлены"

run:
  command: node server.js
  persistenceMount: /data
  containerPort: 3000