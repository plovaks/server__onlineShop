meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 18

build:
  buildCommand: npm install
  buildCommandAdditional: echo "Зависимости установлены"

run:
  runCommand: node server.js
  persistenceMount: /data
  containerPort: 3000